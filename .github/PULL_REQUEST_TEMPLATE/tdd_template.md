<!--
Follow `.copilot-workflows/AGENT_BOOTSTRAP.md` when using AI to author this PR.
Paste the bootstrap line used at the top of this description.
-->

### Bootstrap

_Paste the bootstrap line here (required):_


### Checklist (required)
- [ ] I have included the bootstrap line from `.copilot-workflows/AGENT_BOOTSTRAP.md` in this PR description.
- [ ] There is at least one commit with message starting with `RED:` (e.g., `RED: fizzbuzz - test para 3 === 'fizz'`).
- [ ] There is at least one commit with message starting with `GREEN:`.
- [ ] There is at least one commit with message starting with `REFACTOR:` (can be added later on a follow-up commit).
- [ ] Tests cover the feature and pass locally/CI.

### Description
Describe the change briefly and the TDD steps taken.

### Tests
Explain how the tests demonstrate the TDD cycle (what was RED, what was GREEN, what was REFACTOR).
